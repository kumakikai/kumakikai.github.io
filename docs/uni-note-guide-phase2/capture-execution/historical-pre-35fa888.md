# Uni:Note 3.5.0 build 6 撮影実行状況

## 2026-09-20 Simulator撮影追補

ユーザーのSimulator撮影指示に従い、専用iPad Pro 13-inch M5／iOS 26.5を作成。実UIで「経済学／第8回 マクロ経済」を作成した。

**候補原本6/86、公開版一致確認済み0、approved 0、掲載0/111。未完了。**

撮影IDはUN35-018（ペン）、019（蛍光ペン）、020（消しゴム）、042（録音開始前）、046（文字起こし言語）、052（付箋設定）。`raw/`に無加工・全画面PNG（2752×2064）を保存。crop・合成・画像生成は行っていない。

使用した既存Simulatorアプリは2026-09-13リリース検証のDebug版（800c0c3関連、3.5.0／ローカルbuild 1）。公開build 6のバイナリではない。実際に「その他」に開発用の `Simulate CH-001 recovery` が表示され、ナビゲーション／パレットの説明文にも公開アーカイブとの差分がある。公開版の「プレビューの未使用部分はノートでは表示されず…」等がこの版にはない。

そのため残り80原本の撮影と6候補の公開採用は保留。公開build 6対応ソースを特定してRelease構成でSimulatorを準備してから再開する。指描画を含む現在のアプリ作業ツリーを代用しない。実AI・録音・Pencil・外部アプリ等も対象ごとに条件確認が必要。

`capture-status.json`に87論理IDの状態、取得原本のパス・寸法・SHA256・撮影版を記録。027は087の参照エイリアス。111箇所は引継ぎ目標値で、掲載先の完全照合は未完了。既存7画像、公開画像台帳、Web本文、翻訳、アプリ本体は未変更。

Simulator実操作とPNG寸法・hash・台帳一致を確認。組み込み未実施のため、組み込み後の28ページ／6言語、PC／モバイル、production build・リンク・SEOは未実施。commit／push／deployなし。

以下はSimulator指定前の調査記録。実機接続は現在の再開必須条件ではない。

## 当初の実機調査（Simulator指定前）

当初の撮影成功 **0/86**、新規approved **0**、新規掲載 **0/111**。

## 確認結果

- Web作業ツリーは開始時clean。アプリ作業ツリーには指描画などの並行変更があり、変更していない。
- 公開build証跡のアーカイブが存在し、実バイナリSHA256は記録と一致。
- vtoolで実機用 `IOS` を確認。Simulator用ではない。
- devicectlでiPad実機は `unavailable`、利用可能な実機はiPhoneのみ。iPadの個別照会でも接続不可を確認。
- 既存Simulatorと3.4.0撮影支援を発見したが、公開build 6の代用として採用しない。
- 公開build 6とソースの完全一致は既存証跡でも未証明。最新コードを再ビルドしてbuild 6と呼ばない。

## 未完了台帳

[86原本＋1エイリアスの状況](capture-status.json)。各IDに目的、未完了理由、準備状況、再開条件を記録。撮影ファイル・crop・実撮影版は未取得のためnull。

全UN35-001〜087が未完了。UN35-027はUN35-087のエイリアスで物理撮影対象外。既存7画像はすべて保持し、採用statusは変更していない。

## 検証と公開

新画像がないため、画像組み込み後の28ページ・6言語・PC/モバイル・検索・Support・SEO・リンク・production build検証は未実施。画像品質監査も対象原本0枚で未実施。以前の検証結果を今回のPASSとして扱わない。

変更は本記録とcapture-status.jsonだけ。Web本文・画像・公開画像台帳・アプリコードは未変更。commit/push/deployなし。

## 再開条件

撮影用iPadを接続し、公開3.5.0 build 6の実行、教材専用データ環境、UI操作と原本取得経路を確認する。現状の登録端末は11インチで、計画の13インチ環境は未確認。Pencil、録音・文字起こし、実AI、Premium/iCloud、外部アプリ/PiPは各対象の利用可否を確認してから撮影する。
